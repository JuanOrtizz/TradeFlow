<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TradeFlow.Views.LocalidadesView"
             xmlns:controls="clr-namespace:TradeFlow.Controls"
             Title="Localidades"
             BackgroundColor="{StaticResource Background}">

    <Grid RowDefinitions="Auto, *" Padding="20">

        <!-- Header -->
        <Grid Grid.Row="0" Padding="0" RowDefinitions="*" ColumnDefinitions="*">
            
            <Button 
                HorizontalOptions="End"
                Text="Agregar"
                Command="{Binding IrAAgregarCommand}"
                Style="{StaticResource PrimaryButtonStyle}"
                Padding="12"
                CornerRadius="12"
                FontSize="16"
                />
        </Grid>

        <!-- Lista de localidades -->
        <CollectionView 
            Grid.Row="2"
            ItemsSource="{Binding ListaLocalidades}"
            IsVisible="{Binding IsBusy, Converter={StaticResource InvertBoolConverter}}"
            Margin="0,8,0,0"
            >

            <CollectionView.EmptyView>
                <Label Text="No hay localidades registradas"
                       Style="{StaticResource EmptyStateStyle}"
                       Margin="20,40" />
            </CollectionView.EmptyView>


            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical"
                           ItemSpacing="5" />
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate>

                    <Border Style="{StaticResource CardStyle}"
                               Padding="16">

                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="14">

                            <!-- Nombre -->
                            <Label Grid.Column="0"
                                Text="{Binding Nombre}"
                                FontSize="16"
                                FontAttributes="Bold"
                                TextColor="{StaticResource TextPrimary}"
                                VerticalOptions="Center" />

                            <Button
                                Grid.Column="1"
                                Text="Eliminar"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.EliminarCommand}"
                                CommandParameter="{Binding .}"
                                BackgroundColor="{StaticResource Danger}"
                                TextColor="White"
                                FontAttributes="Bold"
                                >

                            </Button>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <controls:CargandoOverlay EstaCargando="{Binding IsBusy}" />

    </Grid>

</ContentPage>
